import { FilterwordPipe } from './filterword.pipe';

describe('FilterwordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterwordPipe();
    expect(pipe).toBeTruthy();
  });
});
